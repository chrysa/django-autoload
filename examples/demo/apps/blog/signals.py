"""Auto-imported by django-autoload on ready() — no manual import needed.

In a real app this is where you connect signal receivers. Here it just records
that it was loaded so the demo can prove auto-import happened.
"""

from __future__ import annotations

from apps import LOADED_COMPONENTS

LOADED_COMPONENTS.append("blog.signals")
